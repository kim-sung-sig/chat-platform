spring.application.name=chat-server
server.port=20001

# ?? ???? (?? ? -Dspring.profiles.active=prod ?? override ??)
spring.profiles.active=dev

# Spring Cloud Config
spring.cloud.config.enabled=false
spring.config.import=optional:configserver:http://localhost:8888,optional:classpath:application-security.properties

# Eureka Client
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.instance.prefer-ip-address=true
eureka.instance.instance-id=${spring.application.name}:${server.port}

# JWT Security
security.jwt.secret-key=${JWT_SECRET_KEY:your-256-bit-secret-key-change-this-in-production-environment-please-use-strong-key}

# DataSource driver (Source: Write / Replica: Read)
spring.datasource.source.driver-class-name=org.postgresql.Driver
spring.datasource.replica.driver-class-name=org.postgresql.Driver

# JPA ??
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.format_sql=true
spring.flyway.enabled=false

# Kafka Producer
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer

# Kafka Consumer (read-receipt-events, member-left-events)
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.group-id=chat-server-group
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.enable-auto-commit=false
spring.kafka.listener.ack-mode=MANUAL_IMMEDIATE

# File Upload
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=50MB

# Virtual Threads (Java 21)
spring.threads.virtual.enabled=true

# Jackson
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=Asia/Seoul

# Micrometer Tracing
management.tracing.sampling.probability=1.0

# Logback
logging.config=classpath:logback-spring.xml

# DataSource URL / JPA show-sql / Kafka / Redis / Logging -> ????? ?? (application-dev.properties / application-prod.properties)
